# ── Core (required) ──────────────────────────────────────
beautifulsoup4>=4.12
lxml>=5.0                   # BS4 parser backend (fast C-based)
requests>=2.31              # HTTP fetching (static pages)

# ── JS AST parsing (primary — strongly recommended) ─────
tree-sitter>=0.23
tree-sitter-javascript>=0.23

# ── SPA rendering (required for LinkedIn, Twitter, etc.) ─
playwright>=1.40            # pip install playwright && playwright install chromium

# ── Environment / CLI (used by env_loader.py) ───────────
python-dotenv>=1.0
questionary>=2.0